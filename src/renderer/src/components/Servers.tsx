import React from 'react';

const Servers: React.FC = () => {
  return (
    <div>
      <h2>Servers Page</h2>
      <p>This is a placeholder for servers list</p>
    </div>
  );
};

export default Servers;
